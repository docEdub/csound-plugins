{
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "/usr/local/bin/csound",
            "args": ["-odac", "websockets/test.csd"],
            "stopAtEntry": false,
            "MIMode": "lldb",
            "preLaunchTask": "Build"
        }
    ]
}
